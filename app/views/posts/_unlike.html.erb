<%= form_for load_reacted, html: { method: :delete , remote: true }, remote: true do |f| %>
  <%= f.button nil, class: "w-12 h-12 hover:bg-blue-100 rounded round" do %>
    <i class="fas fa-heart text-rose-600 text-xl"></i>
  <% end %>
<% end %>
