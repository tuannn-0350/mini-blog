<%= form_for current_user.active_relationships.build, html: { remote: true, class: "w-full" } do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("follow"), class: "w-full p-2" %>
<% end %>
