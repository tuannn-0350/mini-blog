<% provide :title, t("users") %>
<div class="flex flex-col justify-between items-center w-8/12 mx-auto">
  <table class="table-auto w-full">
    <thead>
      <tr>
        <th><%= t("name") %></th>
        <th><%= t("email") %></th>
      </tr>
    </thead>

    <tbody>
      <%= render @users %>
    </tbody>
  </table>
  <%== pagy_nav(@pagy) %>
</div>
